// #include "sgl_patterns.h"
// #include "sgl_draw.h"
// #include "sgl_ppm.h"
// #include "sgl_errors.h"

// #include <string.h>

// void checker_pattern(uint32_t *pixels, size_t pixel_w, size_t pixel_h, size_t x, size_t y, size_t w, size_t h, uint32_t background_color, uint32_t forground_color, const char* file_path)
// {
//     sgl_fill(pixels, w, h, background_color);

//     for (int y = 0; )

//     Errno err = sgl_ppm_write(pixels, w, h, file_path);
//     if (err) {
//         fprintf (stderr, "ERROR: could not save file %s: %s\n", file_path, strerror(errno));
//         return false;
//     }
//     return true;
// }
